# V1.1.4B 最小托盘程序真人验证记录

> 日期：2026-05-02
> 类型：docs-only validation record
> 对应实现 commit：`2eb2c5e` feat: add minimal tray app

## 1. 验证范围

V1.1.4B 只验证最小托盘入口，不验证启动 / 停止 / 重启小黄。

已验证菜单项：

- 打开设置
- 打开日志目录
- 关于/状态
- 退出托盘

明确不在本阶段验证：

- 启动小黄
- 停止小黄
- 重启小黄
- STT server / voice_overlay 进程检测
- 开机自启
- 安装器

## 2. 真人验证结果

| 验证项 | 结果 |
|--------|------|
| 托盘图标能出现 | 通过 |
| 右键菜单能打开 | 通过 |
| 点击“打开设置”能打开 Settings UI | 通过 |
| Settings UI 能读取 `config_settings_ui_test.json` | 通过 |
| 点击“打开日志目录”能打开 `logs/` 目录 | 通过 |
| 点击“关于/状态”正常 | 通过 |
| 点击“退出托盘”能退出托盘程序 | 通过 |
| V1.1.4B 没有启动/停止/重启小黄 | 符合预期 |
| 退出托盘不会停止 STT server / voice_overlay | 符合预期 |
| 未影响 voice_overlay / wake / session / TTS / LLM router 主链路 | 符合预期 |

结论：V1.1.4B 最小托盘入口已通过本机真人验证，可以阶段性收口。

## 3. 边界确认

- 本次验证记录不修改运行代码。
- 本次验证记录不修改 PowerShell 脚本。
- 本次验证记录不写 `E:\DataBase`。
- 本次验证记录不写真实 API key。
- 托盘退出语义保持为“只退出托盘程序”，不停止 STT server 或语音悬浮窗。

## 4. 后续建议

V1.1.4C 再实现启动 / 停止 / 重启控制，并继续复用现有：

- `scripts/start_xiaohuang.ps1`
- `scripts/stop_xiaohuang.ps1`
- `scripts/start_overlay.ps1`

不要把 V1.1.4C 的进程控制逻辑回填到 V1.1.4B。
